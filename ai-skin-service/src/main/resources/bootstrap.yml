spring:
  cloud:
    config:
      profile: profiles.active
      label: master
      username:
      password:
      fail-fast: true
      retry:
        max-interval: 2000
        max-attempts: 5
        multiplier: 1.1
        initial-interval: 1000
      name: skin
      enabled: false
      uri:
management:
  security:
    enabled: false
jasypt:
  encryptor:
    password:
